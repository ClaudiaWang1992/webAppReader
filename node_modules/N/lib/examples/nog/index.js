(function(){
  var Connect, N, server;
  Connect = require('connect');
  N = require('../../index');
  N.mountController(require('./app/posts'), "/posts");
  server = Connect.createServer(Connect.logger(), N.connect());
  server.listen(8080);
})();
